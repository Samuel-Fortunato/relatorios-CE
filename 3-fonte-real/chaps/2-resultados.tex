\section{Resultados}

\subsection{Montagem 1}
Fez se a montagem inicial, apresentada na \cref{circ:intro1}, utilizando a placa de resistências disponível no laboratório.

Pretendia-se determinar a resistência de entrada do circuito formado pela placa de resistências, em duas situações: a) com os terminais C e D desconectados, b) com os terminais C e D conectados.
Para tal, fez se variar a corrente de entrada, fornecida pela fonte de tensão aos terminais A e B, e foi se registando a diferença de potencial entre estes mesmos terminais. Para esse efeito recorreu-se a um reóstato.

\begin{figure}[!ht]
	\centering
	\begin{circuitikz}[american voltages]
		\draw (0,0) to [vR] ++(3,0)
		to [ammeter] ++(2,0)
		coordinate (Vp)
		-- ++(2,0)
		node[circ, label={above:A}] (A) {}
		to [R={$R_1=\qty{120}{\ohm}$}] ++(3,0)
		coordinate (Ra)
		to [R={$R_3=\qty{270}{\ohm}$}] ++(0,-4)
		coordinate (Rb)
		to [R={$R_4=\qty{120}{\ohm}$}] ++(-3,0)
		node[circ, label={below:B}] (B) {}
		-- ++(-2,0)
		coordinate (Vn)
		-- ++(-5,0)
		to [battery1, v<={$\qty{15}{V}$}, invert] (0,0);

		\draw (Ra) to [R={$R_2=\qty{330}{\ohm}$}] ++(3,0)
		node[circ, label={above:C}] (C) {};

		\draw (Rb) -- ++(3,0)
		node[circ, label={below:D}] (D) {};

		\draw (Vp) to [voltmeter] (Vn);

		\draw[dashed, thick, blue] ($(A)+(-0.5,1)$) rectangle ($(D)+(0.5,-1)$);
		\node[blue] at ($(A)!0.5!(C) + (0,1.3)$) {\textbf{Placa de resistências}};
	\end{circuitikz}
	\caption{Montagem inicial}
	\label{circ:intro1}
\end{figure}

\begin{table}[!ht]
	\centering
	\caption{Valor das Resistências}
	\label{tab:resist-exper}
	\begin{tabular}{c|c|c}
		      & Valor teórico   & Valor experimetal \\
		\hline
		$R_1$ & \qty{120}{\ohm} & \qty{120.5}{\ohm} \\
		$R_2$ & \qty{330}{\ohm} & \qty{327}{\ohm}   \\
		$R_3$ & \qty{270}{\ohm} & \qty{260}{\ohm}   \\
		$R_4$ & \qty{120}{\ohm} & \qty{120.6}{\ohm}
	\end{tabular}
\end{table}

\subsubsection{Caso a)}

Nesta configuração, os pontos C e D estão conectados. Establecendo a ligação entre estes terminais, note-se que as resistências $R_2$ e $R_3$ passam a estar em paralelo. Assim, o circuito equivalente encontra-se representada no \cref{circ:1a}.

\begin{figure}[!ht]
	\centering
	\begin{circuitikz}[american voltages]
		\draw (0,0) to [vR] ++(2,0)
		to [ammeter] ++(1,0)
		-- ++(0.5,0)
		coordinate (Vp)
		% -- ++(0,0)
		% node[circ, label={above:A}] (A) {}
		to [R={$R_1=\qty{120}{\ohm}$}] ++(2,0)
		coordinate (Ra)
		to [R={$R_3=\qty{270}{\ohm}$}] ++(0,-2)
		coordinate (Rb)
		to [R={$R_4=\qty{120}{\ohm}$}] ++(-2,0)
		% node[circ, label={below:B}] (B) {}
		% -- ++(-2,0)
		coordinate (Vn)
		-- ++(-3.5,0)
		to [battery1, v<={$\qty{15}{V}$}, invert] (0,0);

		\draw (Ra) to [R={$R_2=\qty{330}{\ohm}$}] ++(3,0)
		-- ++(0,-2)
		-- (Rb);

		\draw (Vp) to [voltmeter] (Vn);
	\end{circuitikz}
	\caption{Circuito caso a)}
	\label{circ:1a}
\end{figure}

Os valores registados da tensão e da corrente constam na \cref{tab:mont1-alinA}.
\begin{table}[!ht]
	\centering
	\caption{Tensão e corrente caso a)}
	\label{tab:mont1-alinA}
	% Use pgfplotstable to render the CSV
	\pgfplotstabletypeset[
		col sep=comma, % Tells LaTeX the file is a CSV
		% string type,   % Treat headers as text
		% Define the full grid (vertical lines)
		column type={|c|},
		% Styling the header and adding the horizontal lines for the "Full Grid"
		every head row/.style={
				before row=\hline,
				after row=\hline
			},
		every last row/.style={
				after row=\hline
			},
		% Assigning column headers and units
		columns/Current/.style={
				column name={Corrente (\unit{\milli\ampere})},
				column type={|c|},
				% column type={S[table-format=2.1]|} % Adjust format to your max digits
			},
		columns/Voltage/.style={
				column name={Tensão (\unit{\volt})},
				column type={c|},
				% column type={|S[table-format=2.2]|} % Adjust format to your max digits
			},
	]{dados/1a.csv}
\end{table}

\subsubsection{Caso b)}
Nesta configuração, os terminais C e D estão desconectados, logo não ha corrente tanto nos ramos do circuito que conectam a estes terminais, como na resistência $R_2$.
De tal modo, as zonas onde não há corrente podem efetivamente ser eliminadas, obtendo-se assim o circuito equivalente representado na \cref{circ:1b}.

\begin{figure}[!ht]
	\centering
	\begin{circuitikz}[american voltages]
		\draw (0,0) to [vR] ++(2,0)
		to [ammeter] ++(1,0)
		-- ++(0.5,0)
		coordinate (Vp)
		% -- ++(0,0)
		% node[circ, label={above:A}] (A) {}
		to [R={$R_1=\qty{120}{\ohm}$}] ++(2,0)
		coordinate (Ra)
		to [R={$R_3=\qty{270}{\ohm}$}] ++(0,-2)
		coordinate (Rb)
		to [R={$R_4=\qty{120}{\ohm}$}] ++(-2,0)
		% node[circ, label={below:B}] (B) {}
		% -- ++(-2,0)
		coordinate (Vn)
		-- ++(-3.5,0)
		to [battery1, v<={$\qty{15}{V}$}, invert] (0,0);

		% \draw (Ra) to [R={$R_x=\qty{0}{\ohm}$}] ++(2,0)
		% node[circ, label={above:C}] (C) {};

		% \draw (Rb) -- ++(2,0)
		% node[circ, label={below:D}] (D) {};

		\draw (Vp) to [voltmeter] (Vn);
	\end{circuitikz}
	\caption{Circuito caso b)}
	\label{circ:1b}
\end{figure}

As medições de corrente e tensão encontram-se na \cref{tab:mont1-alinB}.
\begin{table}[!ht]
	\centering
	\caption{Tensão e corrente caso b)}
	\label{tab:mont1-alinB}
	% Use pgfplotstable to render the CSV
	\pgfplotstabletypeset[
		col sep=comma, % Tells LaTeX the file is a CSV
		% string type,   % Treat headers as text
		% Define the full grid (vertical lines)
		column type={|c|},
		% Styling the header and adding the horizontal lines for the "Full Grid"
		every head row/.style={
				before row=\hline,
				after row=\hline
			},
		every last row/.style={
				after row=\hline
			},
		% Assigning column headers and units
		columns/Current/.style={
				column name={Corrente (\unit{\milli\ampere})},
				column type={|c|},
				% column type={S[table-format=2.1]|} % Adjust format to your max digits
			},
		columns/Voltage/.style={
				column name={Tensão (\unit{\volt})},
				column type={c|},
				% column type={|S[table-format=2.2]|} % Adjust format to your max digits
			},
	]{dados/1b.csv}
\end{table}



\subsection{Montagem 2}

Na segunda parte do trabalho, o objetivo foi simular uma fonte de tensão real, ou seja uma fonte que apresenta uma resistência interna. Para isso, foi ligada a fonte de tensão diretamente à placa de resistências usada nas montagens anteriores, como consta na \cref{circ:fonte-real-sim}.


Para simplificar esta análise, recorreu-se ao teorema de Thévenin, que permitiu substituir toda a complexidade da placa de resistências por um circuito equivalente muito mais simples. Este modelo reduz a montagem a apenas uma fonte de tensão ideal $V_{th}$ associada em série a uma única resistência $R_{th} = R_s$, representando exatamente o comportamento que observamos nos pontos de ligação da carga (terminais 1 e 2).

Para simplificar esta análise, recorreu-se ao teorema de Thévenin, que permitiu substituir toda a complexidade da placa de resistências por um circuito equivalente muito mais simples. Este modelo reduz a montagem a apenas uma fonte de tensão ideal $V_{th}$ associada em série a uma única resistência $R_{th} = R_s$, representando exatamente o comportamento que observamos nos pontos de ligação da carga (terminais 1 e 2). O circuito de Thévenin genérico encontra-se representado na \cref{circ:equiv-thevenin}:
\vspace{10pt}

\begin{figure}[!ht]
	\centering
	\begin{circuitikz}[american voltages]
		\draw (0,0) node[ocirc, label={above:1}] (out1) {}
		-- ++(-1,0)
		to [R, l_={$R_{Th}$}] ++(-3,0)
		to [battery1, l_={$V_{Th}$}] ++(0,-2)
		-- ++(4,0)
		node[ocirc, label={above:2}] (out2) {};
	\end{circuitikz}
	\caption{Circuito equivalente de Thévenin}
	\label{circ:equiv-thevenin}
\end{figure}

\begin{figure}[!ht]
	\centering
	\begin{circuitikz}[american voltages]
		\draw (0,0) to [battery1, v=15V, name=mainbat] ++(0,-4)
		-- ++(2,0)
		node[circ, label={above:B}] (B) {}
		to [R, l_={$R_4=\qty{120}{\ohm}$}] ++(2,0) coordinate(placaS)
		to [R={$R_3=\qty{270}{\ohm}$}] ++(0,4) coordinate(placaN)
		to [R={$R_1=\qty{120}{\ohm}$}] ++(-2,0)
		node[circ, label={above:A}] (A) {}
		-- (0,0);

		\draw (placaN) to [R, l_={$R_2=\qty{330}{\ohm}$}] ++(2,0)
		node[circ, label={above:C}] (C) {}
		-- ++(2,0)
		node[ocirc, label={above:1}] (out1) {};

		\draw (placaS) -- ++(2,0)
		node[circ, label={above:D}] (D) {}
		-- ++(2,0)
		node[ocirc, label={above:2}] (out2) {};

		\node[draw, dashed, red, thick, inner sep=20,
			fit=(A) (B) (C) (D),
			label={[blue, font=\bfseries]above:Placa de resistências}] (innerRect) {};

		\node[draw, dashed, blue, thick, inner sep=20,
			fit=(mainbat) (innerRect),
			label={[blue, font=\bfseries]above:Fonte real simulada}] (outerRect) {};
	\end{circuitikz}
	\caption{Fonte real simulada}
	\label{circ:fonte-real-sim}
\end{figure}

Com esta montagem, foi medida a diferença de potencial nos terminais 1 e 2, $V_0$ e a corrente de curto-circuito $I_{sc}$ que atravessa a resistência equivalente, tendo-se obtido $V_0 = 7.01 \pm 0.01$V e $I_{sc} = 16.93 \pm 0.01$ mA.

\vspace{10pt}

Numa fonte real, tem-se que a tensão de saída, neste caso a tensão nos terminais 1 e 2, diminui à medida que a corrente debitada por esta aumenta. Com o intuito de calcular a resistência de saída da fonte simulada, fez-se variar a corrente através de um reostáto ligado aos terminais 1 e 2, e foi registado o conjunto de valores tensão-corrente no mesmo, que se encontram na \cref{tab:mont2}.

\begin{figure}[!ht]
	\centering
	\begin{circuitikz}[american voltages]
		\draw (0,0) to [battery1, v=15V, name=mainbat] ++(0,-4)
		-- ++(1,0)
		node[circ, label={above:B}] (B) {}
		to [R, l_={$R_4=\qty{120}{\ohm}$}] ++(2,0) coordinate(placaS)
		to [R, l_={$R_3=\qty{270}{\ohm}$}] ++(0,4) coordinate(placaN)
		to [R={$R_1=\qty{120}{\ohm}$}] ++(-2,0)
		node[circ, label={above:A}] (A) {}
		-- (0,0);

		\draw (placaN) to [R, l_={$R_2=\qty{330}{\ohm}$}] ++(2,0)
		node[circ, label={above:C}] (C) {}
		-- ++(1,0)
		node[ocirc, label={above:1}] (out1) {};

		\draw (placaS) -- ++(2,0)
		node[circ, label={above:D}] (D) {}
		-- ++(1,0)
		node[ocirc, label={above:2}] (out2) {};

		\draw (out1) to [ammeter] ++(2,0)
		coordinate (reostP)
		to [vR] ++(0,-4)
		coordinate (reostN)
		-- (out2);

		\draw (reostP) -- ++(1,0)
		to [voltmeter] ++(0,-4)
		-- (reostN);


		\node[draw, dashed, blue, thick, inner sep=20,
			fit=(mainbat) (A) (B) (C) (D),
			label={[blue, font=\bfseries]above:Fonte real simulada}] (outerRect) {};
	\end{circuitikz}
	\caption{Segunda montagem}
	\label{circ:montagem2}
\end{figure}


\begin{table}[!ht]
	\centering
	\caption{Tensão e corrente no reóstato.}
	\label{tab:mont2}
	% Use pgfplotstable to render the CSV
	\pgfplotstabletypeset[
		col sep=comma, % Tells LaTeX the file is a CSV
		% string type,   % Treat headers as text
		% Define the full grid (vertical lines)
		column type={|c|},
		% Styling the header and adding the horizontal lines for the "Full Grid"
		every head row/.style={
				before row=\hline,
				after row=\hline
			},
		every last row/.style={
				after row=\hline
			},
		% Assigning column headers and units
		columns/Current/.style={
				column name={Corrente (\unit{\milli\ampere})},
				column type={|c|},
				% column type={S[table-format=2.1]|} % Adjust format to your max digits
			},
		columns/Voltage/.style={
				column name={Tensão (\unit{\volt})},
				column type={c|},
				% column type={|S[table-format=2.2]|} % Adjust format to your max digits
			},
	]{dados/2.csv}
\end{table}
\vspace{100pt}

Os valores medidos apresentam incertezas inerentes aos instrumentos de medição utilizados, voltímetro e amperímetro de $\pm 0,1$ V e $\pm 0,1$ mA, respetivamente.